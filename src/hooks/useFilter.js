import { useEffect, useState } from "react";
import {useFetchMarkers} from './'


export const useFilter = () => {

  const [filter, setFilter] = useState([]);
 

useEffect(() => {

}, [])


return {
    filter
}

}
